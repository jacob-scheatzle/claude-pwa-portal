{$SITE_URL:localhost} {
	reverse_proxy portal:8000

	encode zstd gzip

	header {
		# Basic security headers; child-app CSP comes later when SDK lands.
		X-Content-Type-Options nosniff
		Referrer-Policy strict-origin-when-cross-origin
	}
}
