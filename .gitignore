__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.env
.env.local

data/
*.db
*.db-journal

# Build artifacts (released as GitHub release assets, not committed)
pwa-portal-app.zip

.DS_Store
.idea/
.vscode/

# Claude Code per-developer settings. Shared project config (e.g. a checked-in
# .claude/settings.json or .claude/commands/) can still be committed explicitly.
.claude/settings.local.json
.claude/launch.json
